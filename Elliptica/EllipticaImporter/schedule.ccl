# Schedule definitions for thorn EllipticaImporter

STORAGE: ADMBase::metric[0]
STORAGE: ADMBase::curv[0]
STORAGE: ADMBase::lapse[0]
STORAGE: ADMBase::shift[0]
STORAGE: HydroBase::rho[0]
STORAGE: HydroBase::eps[0]
STORAGE: HydroBase::press[0]
STORAGE: HydroBase::vel[0]

SCHEDULE EllipticaImporter AT initial
{
  LANG: C
  WRITES:
    ADMBase::metric(everywhere),
    ADMBase::curv(everywhere),
    ADMBase::lapse(everywhere),
    ADMBase::shift(everywhere),
    HydroBase::rho(everywhere),
    HydroBase::eps(everywhere),
    HydroBase::press(everywhere),
    HydroBase::vel(everywhere)
  SYNC:
    ADMBase::metric,
    ADMBase::curv,
    ADMBase::lapse,
    ADMBase::shift,
    HydroBase::rho,
    HydroBase::eps,
    HydroBase::press,
    HydroBase::vel
  READS:
    grid::x, grid::y, grid::z
  OPTIONS: meta
} "Import initial data from Elliptica"
